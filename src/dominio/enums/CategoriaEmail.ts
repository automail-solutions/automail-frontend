export enum CategoriaEmail {
  PRODUTIVO = 'Produtivo',
  IMPRODUTIVO = 'Improdutivo'
}

export enum StatusProcessamento {
  CARREGANDO = 'carregando',
  SUCESSO = 'sucesso',
  ERRO = 'erro',
  INATIVO = 'inativo'
}

export enum ModoEntrada {
  ARQUIVO = 'arquivo',
  TEXTO = 'texto'
}